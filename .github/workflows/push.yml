name: AKRO MEDIA BOT RUNNER

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg aria2

      - name: Install & Update Python Dependencies
        run: |
          python -m pip install --upgrade pip
          # تحديث yt-dlp ضروري جداً لأن يوتيوب يغير خوارزمياته يومياً
          pip install --upgrade python-telegram-bot yt-dlp

      - name: Create Downloads Directory
        run: mkdir -p downloads

      - name: Run AKRO Bot
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        run: |
          # إضافة علم (Flag) لضمان عدم توقف الأكشن فجأة
          python bot/bot.py
